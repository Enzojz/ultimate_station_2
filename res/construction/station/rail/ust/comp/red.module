local updateFn = function(result, transform, tag, slotId, addModelFn, params)
end

function data()
    return {
        availability = {
            yearFrom = 0,
            yearTo = 0,
        },
        buildMode = "SINGLE",
        cost = {
            price = 0,
        },
        description = {
            name = _("MENU_MODULE_COLOR_RED"),
            description = _("MENU_MODULE_COLOR_RED_DESC"),
        },
        category = {
            categories = {"ust_cat_comp"},
        },
        type = "ust_color",
        order = {
            value = 2,
        },
        metadata = {
            color = "red",
            typeId = 29,
            typeName = "ust_color",
            scriptName = "construction/station/rail/ust/comp/color",
            preProcessAdd = "preProcessAdd",
            preProcessRemove = "preProcessRemove",
            slotSetup = "slotSetup",
            classify = "classify"
        },
        
        updateFn = updateFn,
        
        getModelsFn = function(params)
            return {}
        end
    }

end
